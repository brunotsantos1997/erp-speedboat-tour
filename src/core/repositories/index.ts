// src/core/repositories/index.ts
// Offline-first repositories initialization
export const initializeOfflineRepositories = async () => {
  // No explicit initialization needed here as it happens on login via SyncManager
};
